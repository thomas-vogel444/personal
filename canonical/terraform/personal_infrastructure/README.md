# Provisioning infrastructure using GCP

Prerequisites:
- a project
- a GCS bucket with a unique name
-  

Login:
Before invoking terraform, you first need to authenticate using `gcloud auth application-default login`

